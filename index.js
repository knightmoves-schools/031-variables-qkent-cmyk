// Define the courseLength variable and assign it the value 20
const courseLength = 20;

// Assign the innerHTML of the element with id 'result' to courseLength
document.getElementById('result').innerHTML = courseLength;
